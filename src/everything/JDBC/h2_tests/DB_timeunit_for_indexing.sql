TIMEUNIT
"
  CREATE TABLE "table_name"
   (
	"TIMEUNITID" NUMBER(11,0),
   )

"
  CREATE TABLE "TIMEUNIT"
   (	"ID" NUMBER(11,0) NOT NULL ENABLE,
	"UNITYEAR" NUMBER(11,0) NOT NULL ENABLE,
	"UNITMONTHOFYEAR" NUMBER(11,0) NOT NULL ENABLE,
	"UNITMONTH" NUMBER(11,0) NOT NULL ENABLE,
	"UNITWEEKOFYEAR" NUMBER(11,0) NOT NULL ENABLE,
	"UNITWEEK" NUMBER(11,0) NOT NULL ENABLE,
	"UNITDATE" NUMBER(11,0) NOT NULL ENABLE,
	"UNITDAYOFWEEK" NUMBER(11,0) NOT NULL ENABLE,
	"WEEKDAY" NUMBER(1,0) NOT NULL ENABLE,
	"UNITTIMESTAMP" DATE,
	"TRG_CREATEDON" TIMESTAMP (6),
	"TRG_LASTUPDATEDON" TIMESTAMP (6),
	 CONSTRAINT "TIMEUNIT" PRIMARY KEY ("ID")
	 )
